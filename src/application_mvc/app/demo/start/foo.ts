import { Client } from "discord.js";

export default class Foo 
{
    public execute(client: Client)
    {
        console.log("Diese funktion wird zum Start 1x ausgeführt für eigene Initialisierungsprozesse")
    }
}